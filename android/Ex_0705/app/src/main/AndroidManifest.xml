<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ex_0705"
        tools:targetApi="31">
        <activity
            android:name=".RelativeActivity"
            android:exported="true" >
            <intent-filter>

                <!-- intent-filter : 첫번째로 실행이 될 가능성이 있는 어플리케이션이다 -->
                <!-- 만약 intent-filter 가 여러게 있다면 가장먼저 만들어진 요소에서 실행 -->
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FrameActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".LinearActivity"
            android:exported="true">

            <!--
            exported : 다른 어플에 의해 현재 만들어 지고 있는 앱의 액티비티가
            실행 될 수 있는지 아닌 지를 설정 하는 역할
            -->
        </activity>
        <activity
            android:name=".FirstActivity"
            android:exported="true" />
    </application>

</manifest>